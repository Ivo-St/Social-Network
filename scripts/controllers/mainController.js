/* global socialNetwork, sessionStorage */

socialNetwork.controller('mainController', function ($scope) {

    // todo see if this is good practise
    /*if (sessionStorage.username) {
    $scope.ownProfileData = JSON.parse(sessionStorage.ownProfileData);
    $scope.ownProfileData.profileImage = 'data:image/jpeg;base64,' + $scope.ownProfileData.profileImage;
}*/

    //console.log($scope.ownProfileData);
});
